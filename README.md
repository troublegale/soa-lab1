# ITMO SOA Lab 1
Lab work #1 of a System-Oriented Architecture course at ITMO University
---
Run: `docker compose -f 'docker-compose.yaml' up -d --build 'swagger-ui'`

Access: `http://localhost:8080/`
services:
    swagger-ui:
        image: swaggerapi/swagger-ui
        ports:
            - "8080:8080"
        environment:
            SWAGGER_JSON: /mnt/openapi.yaml
        volumes:
            - ./openapi.yaml:/mnt/openapi.yaml